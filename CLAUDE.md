# CLAUDE.md — SMCC Growth Coach 작업 규칙

이 파일은 Claude Code가 이 프로젝트에서 항상 지켜야 할 규칙을 정의한다.
모든 작업은 이 규칙을 최우선으로 따른다.

---

## 1. 프로젝트 범위

- **이 프로젝트 폴더(`smcc-growth-ai`) 안에서만 작업한다.**
- `modeun-motors` 프로젝트나 다른 어떤 프로젝트도 절대 건드리지 않는다.
- 파일 읽기, 편집, 삭제, 커밋 모두 `smcc-growth-ai` 범위 안에서만 수행한다.

---

## 2. 무료 도구 정책

- 모든 도구, 라이브러리, 서비스는 무료 플랜 기준으로만 사용한다.
- 유료 SaaS, 유료 API, 유료 템플릿, 유료 이미지, 유료 폰트는 사용하지 않는다.
- 새 패키지나 서비스를 추가할 때 반드시 무료 여부를 확인하고 사용자에게 보고한다.
- Supabase는 Free Plan 제약(행 수, 스토리지, API 호출) 안에서 설계한다.
- 자세한 무료 도구 목록 → [docs/FREE_TOOLS_POLICY.md](docs/FREE_TOOLS_POLICY.md)

---

## 3. SMCC 브랜드 이해

**SMCC는 커피 브랜드가 아니다.**

커피는 사람들을 모이게 하는 자연스러운 매개체일 뿐이다.
SMCC의 핵심은 아침 시간에 개인이 스스로 건강한 변화를 시작하고,
그 변화를 지속할 수 있도록 돕는 커뮤니티다.

### 코드와 UI에서 지켜야 할 원칙

- 컴포넌트 이름, 변수명, 텍스트에 커피/카페/원두 중심 언어를 쓰지 않는다.
- 건강한 변화, 아침 루틴, 커뮤니티, 지속 가능한 웰니스, 사람의 변화 중심으로 표현한다.
- 색상은 브랜드 팔레트(`#00b1cd` 기준)를 따른다. 라떼색/브라운 계열 배경은 사용하지 않는다.
- 디자인이 차가운 SaaS 분석 툴처럼 보이거나, 커피 브랜드처럼 보이거나, 헬스 기록 앱처럼 딱딱하게 보이면 안 된다.

### 금지 표현 (코드 내 텍스트 포함)

- 상위 1% 습관, 인생이 바뀐다, 안 오면 손해, 성공 공식
- 불안감 조장, 자극적 성과 강조
- 커피 중심 카피 (예: "오늘의 원두", "브루잉 방법")

### 권장 표현

- 오늘 아침의 변화, 함께하는 루틴, 작은 시작, 꾸준한 커뮤니티
- 자기 주도적 변화, 부담 없는 참여, 도시적 웰니스

---

## 4. 브랜드 무드 유지

- UI는 아침 감성의 웰니스 커뮤니티가 쓰는 따뜻하고 정돈된 작업실 느낌이어야 한다.
- 디자인 참고 방향: Linear(정돈), Milanote(레퍼런스), Buffer(캘린더), Webflow/Readymag(에디토리얼)
- 새 화면이나 컴포넌트를 추가할 때 브랜드 무드를 해치는지 항상 검토한다.
- 자세한 디자인 시스템 → [docs/DESIGN_SYSTEM.md](docs/DESIGN_SYSTEM.md)

---

## 5. 커밋/푸시 규칙

- **사용자의 명시적 지시가 있을 때만** 커밋하고 푸시한다.
- 자동으로 커밋하거나 푸시하지 않는다.
- 커밋 메시지는 한국어 또는 영어로 명확하게 작성한다.
- 개발 브랜치: `claude/clever-hamilton-wbnq1z`
- main 브랜치에 직접 푸시하지 않는다.

---

## 6. 개발 원칙

- MVP 범위를 벗어난 기능을 임의로 추가하지 않는다.
- Instagram API 자동 연동은 MVP에서 제외한다. 성과 데이터는 수동 입력 기준으로 구현한다.
- 새 기능을 추가하기 전에 [docs/FEATURE_LIST.md](docs/FEATURE_LIST.md)를 확인한다.
- 데이터 모델 변경 시 [docs/DATA_MODEL.md](docs/DATA_MODEL.md)를 먼저 업데이트한다.
- 보안 취약점(SQL Injection, XSS, CSRF 등)이 생기지 않도록 주의한다.

---

## 7. 파일 구조 원칙

```
smcc-growth-ai/
├── src/
│   ├── components/    # 재사용 UI 컴포넌트
│   ├── pages/         # 화면 단위 컴포넌트
│   ├── hooks/         # 커스텀 훅
│   ├── lib/           # 유틸리티, Supabase 클라이언트
│   ├── types/         # TypeScript 타입 정의
│   └── styles/        # 전역 스타일
├── docs/              # 기획/설계 문서
├── public/            # 정적 파일
├── .env.example       # 환경변수 템플릿
├── CLAUDE.md          # 이 파일
└── README.md
```

---

## 8. 질문/불확실한 상황

- 기획 의도가 불명확하거나 브랜드 방향과 충돌이 예상될 때는 구현 전에 사용자에게 먼저 확인한다.
- 무료 도구 정책에 위배되는 상황이 생기면 반드시 보고하고 대안을 제안한다.
